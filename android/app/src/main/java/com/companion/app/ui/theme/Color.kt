package com.companion.app.ui.theme

import androidx.compose.ui.graphics.Color

// Cores suaves e acolhedoras inspiradas no design
val CompanionLightBeige = Color(0xFFF5F1E8)      // Bege claro (fundo principal)
val CompanionSoftBlue = Color(0xFFB8D4E8)        // Azul suave (accent)
val CompanionPeach = Color(0xFFF4D1AE)           // Pêssego/laranja suave (warm accent)
val CompanionLightPeach = Color(0xFFF9E5D3)      // Pêssego muito claro
val CompanionWarmGray = Color(0xFF6B6B6B)        // Cinza quente (texto)
val CompanionDarkGray = Color(0xFF3A3A3A)        // Cinza escuro (texto principal)

// Para tema escuro (opcional)
val CompanionDarkBeige = Color(0xFF2A2720)
val CompanionDarkBlue = Color(0xFF1A3A4A)


